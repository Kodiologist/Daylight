library(ggplot2)

sys.source(getOption("Kodi.R.path"), Kodi <- new.env())
for (name in Kodi$qw(
        qw, ss, ssq,
        samprows,
        dodge, boxp, no.gridlines, geom_densitybw
      ))
    assign(name, Kodi[[name]])
cached = Kodi$mk.cached(c("Kodi", "example-project"))
write.table = Kodi$org.write.table

# --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

source("data.R", local = T)
